export class Technologies {
  public id: number;
  public name: string;
  public icon: string;

  constructor(id: number, name: string, icon:string){
    this.id = id;
    this.name = name;
    this.icon = icon;
  }
}
